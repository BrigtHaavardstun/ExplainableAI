class IDelta:
    # This corresponds to δ in the equation
    def get_complexity_of_subset(self, subset):
        """
        Compute the complexity of the examples given.
        We model the complexity of the shown subsets by giving the labels.

        A human will not struggle to understand what labels are present.

        subset = ["AB", "C", "AD", "B"]
        """
        pass
